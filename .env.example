# API Configuration
VITE_API_BASE_URL=http://localhost:3000

# Note: Admin credentials are now handled securely via backend JWT authentication
# No need to store admin credentials in frontend environment variables
